# The Updated DCR Stakepool Theme

Fork/clone the repo and then run

```sh
$ npm install
```
to install dependencies.

And then run

```sh
$ gulp
```
to build the dist files and

```sh
$ gulp watch
```

to track file changes.
